<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:PunchReha.ViewModels"
             xmlns:models="clr-namespace:PunchReha.Models"
             x:Class="PunchReha.Views.MenuPage"
             x:DataType="vm:MenuViewModel"
             Title="🥊 Punch Reha">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="12">

            <Label Text="🎯 Level wählen"
                   FontSize="24" FontAttributes="Bold" />

            <Label Text="Halte den Bildschirm gedrückt = härterer Schlag"
                   FontSize="12" TextColor="Gray" />

            <CollectionView ItemsSource="{Binding Levels}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:GameLevel">
                        <Border Stroke="LightGray" StrokeShape="RoundRectangle 8"
                                Padding="16" Margin="0,4">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MenuViewModel}}, Path=StartLevelCommand}"
                                    CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>

                            <Grid ColumnDefinitions="48,*,32" ColumnSpacing="12">
                                <Label Grid.Column="0"
                                       Text="{Binding Number}"
                                       FontSize="24" FontAttributes="Bold"
                                       TextColor="#1976D2"
                                       HorizontalOptions="Center" VerticalOptions="Center" />

                                <VerticalStackLayout Grid.column="1">
                                    <Label Text="{Binding Name}"
                                           FontSize="16" FontAttributes="Bold" />
                                    <Label FontSize="11" TextColor="Gray">
                                        <Label.Text>
                                            <MultiBinding StringFormat="{0}s · {1} Richtungen · Speed x{2}">
                                                <Binding Path="DurationSeconds" />
                                                <Binding Path="Directions.Length" />
                                                <Binding Path="SpeedMultiplier" />
                                            </MultiBinding>
                                        </Label.Text>
                                    </Label>
                                    <ProgressBar Progress="{Binding Number}" MaximumHeightRequest="4" />
                                </VerticalStackLayout>

                                <Label Grid.Column="2"
                                       Text="▶" FontSize="20"
                                       TextColor="#1976D2"
                                       HorizontalOptions="Center" VerticalOptions="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label Text="ℹ️ Steuerung" FontSize="16" FontAttributes="Bold" Margin="0,16,0,0" />

            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto,Auto,Auto" RowSpacing="4" ColumnSpacing="8">
                <Label Grid.Row="0" Grid.Column="0" Text="Tap (kurz)" FontAttributes="Bold" FontSize="12" />
                <Label Grid.Row="0" Grid.Column="1" Text="Leichter Schlag (20%)" FontSize="12" TextColor="Gray" />
                <Label Grid.Row="1" Grid.Column="0" Text="Halten (0.5s)" FontAttributes="Bold" FontSize="12" />
                <Label Grid.Row="1" Grid.Column="1" Text="Mittlerer Schlag (50%)" FontSize="12" TextColor="Gray" />
                <Label Grid.Row="2" Grid.Column="0" Text="Halten (1s)" FontAttributes="Bold" FontSize="12" />
                <Label Grid.Row="2" Grid.Column="1" Text="Starker Schlag (80%)" FontSize="12" TextColor="Gray" />
                <Label Grid.Row="3" Grid.Column="0" Text="Halten (2s+)" FontAttributes="Bold" FontSize="12" />
                <Label Grid.Row="3" Grid.Column="1" Text="Maximaler Schlag (100%)" FontSize="12" TextColor="Gray" />
            </Grid>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
